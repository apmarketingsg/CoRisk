// =============================================================================
// *** STUB — Replace "PLACEHOLDER" with the full master prompt text ***
//
// The prompt receives two injected values at runtime:
//   {{COMPANY_NAME}}    — the company being analysed
//   {{RESEARCH_TEXT}}   — web-search findings gathered in the research phase
//
// The model is instructed to call the `generate_report` tool and return a
// structured ReportData JSON object (see types/report.ts).
// =============================================================================

export const MASTER_PROMPT = 'PLACEHOLDER'
